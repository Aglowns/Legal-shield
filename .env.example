# Frontend / Next.js
# NEXT_PUBLIC_API_URL=  (optional; leave empty to use same-origin /api)

# Required on Vercel: URL of your deployed Python backend (e.g. https://your-app.railway.app)
# Without this, /api/analyze returns 503. Deploy backend/ to Railway, Render, or Fly.io first.
# BACKEND_URL=http://localhost:8000
